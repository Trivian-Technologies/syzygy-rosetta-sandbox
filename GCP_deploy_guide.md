# GCP deploy guide (moved)

The full **Google Cloud Run** deployment guide (first-time setup, `deploy/gcp.env`, Secret Manager, scripts, verification, redeploy, API reference, troubleshooting) now lives in one place:

**[`deploy/README.md`](deploy/README.md)**

Use that file as the single source of truth for GCP deployment. This file is kept at the repo root only as a short pointer so older links still resolve.
